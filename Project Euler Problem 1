# Project Euler Problem 1
# compiled in julia version 1.8.5

function sum_multiples(n::Int)
    sum = 0
    for i = 1:n-1
        if i % 3 == 0 || i % 5 == 0
            sum = i + sum
        end
    end
    return sum
end

println(sum_multiples(1000))
